{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pilot",
  "description": "Capability for the tauri-pilot plugin. Only loaded when the cargo feature `pilot` is enabled in a debug build.",
  "windows": ["main"],
  "permissions": ["pilot:default"]
}
